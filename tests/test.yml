# test file for mariadb-server
---
- hosts: localhost
  connection: local
  become: true
  roles:
    - ../../
  vars:
    mariadb_server_databases_present:
      - name: ipsum
      - name: dolor

    mariadb_server_databases_absent:
      - name: sit
      - name: amet

    mariadb_server_users_present_hosts:
      - 'localhost'
      - '%'

    mariadb_server_users_present:
      - name: consectetur
        password: 'elit'
        privs:
          - 'ipsum.*:ALL'
          - 'dolor.*:ALL'
      - name: adipiscing
        password: 'lacus'
        privs:
          - 'ipsum.*:SELECT'
          - 'dolor.*:INSERT,UPDATE'
        hosts:
          - '%'

    mariadb_server_users_absent:
      - name: urna
      - name: vehicula
        hosts:
          - '%'

    mariadb_server_queries:
      - database: mysql
        queries:
          - "INSERT IGNORE INTO user (Host, User, Password, ssl_type, ssl_cipher, x509_issuer, x509_subject) VALUES ('localhost', 'test', password('test'), '', '', '', '')"

    mariadb_server_zoneinfo_manage: true
